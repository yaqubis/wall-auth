# Credenciais do banco de dados PostgreSQL
DB_USR=wallauth
DB_PWD=troque_pela_sua_senha

# Chave secreta JWT (Base64, mínimo 256 bits para HS256)
# Gere com: openssl rand -base64 64
JWT_SECRET=troque_pela_sua_chave_secreta_base64
